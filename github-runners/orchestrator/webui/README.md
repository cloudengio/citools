# Package [github.com/cloudengio/citools/runners/macos/orchestrator/webui](https://pkg.go.dev/github.com/cloudengio/citools/runners/macos/orchestrator/webui?tab=doc)

```go
import github.com/cloudengio/citools/runners/macos/orchestrator/webui
```

Package webui provides primitives to interact with the openapi HTTP API.

Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO
NOT EDIT.

Package webui serves the orchestrator's management web UI and its
OpenAPI-specified JSON API. The API types and server interface in api.gen.go
are generated from openapi.yaml.

## Constants
### BasePath
```go
BasePath = "/api/v1"

```
BasePath is the URL prefix under which the JSON API is served.



## Functions
### Func FrontendAssets
```go
func FrontendAssets(opts ...webassets.AssetsOption) fs.FS
```
FrontendAssets returns the SPA asset filesystem rooted at frontend/dist.
With no options it serves the files embedded in the binary. Pass
reload options (webassets.WithReloading, or webassets.Config.Options /
OptionsFromFlags) to prefer newer files from the local filesystem, so the
UI can be rebuilt with `npm run build` and picked up without recompiling the
binary.

### Func GetSpec
```go
func GetSpec() (swagger *openapi3.T, err error)
```
GetSpec returns the OpenAPI specification corresponding to the generated
code in this file. External references in the spec are resolved through
PathToRawSpec; externally-referenced files must be embedded in their
corresponding Go packages (via the import-mapping feature). URL-based
external refs are not supported.

### Func GetSpecJSON
```go
func GetSpecJSON() ([]byte, error)
```
GetSpecJSON returns the raw JSON bytes of the embedded OpenAPI
specification: decompressed but not unmarshaled. External references are
not resolved here; the bytes are the spec exactly as embedded by codegen.
The result is cached at package init time, so repeated calls are cheap.

### Func GetSwagger
```go
func GetSwagger() (*openapi3.T, error)
```
GetSwagger returns the OpenAPI specification corresponding to the generated
code in this file.

Deprecated: GetSwagger predates kin-openapi renaming openapi3.Swagger to
openapi3.T. Use [GetSpec] instead. This wrapper is retained for backwards
compatibility.

### Func Handler
```go
func Handler(si ServerInterface) http.Handler
```
Handler creates http.Handler with routing matching OpenAPI spec.

### Func HandlerFromMux
```go
func HandlerFromMux(si ServerInterface, m ServeMux) http.Handler
```
HandlerFromMux creates http.Handler with routing matching OpenAPI spec based
on the provided mux.

### Func HandlerFromMuxWithBaseURL
```go
func HandlerFromMuxWithBaseURL(si ServerInterface, m ServeMux, baseURL string) http.Handler
```

### Func HandlerWithOptions
```go
func HandlerWithOptions(si ServerInterface, options StdHTTPServerOptions) http.Handler
```
HandlerWithOptions creates http.Handler with additional options

### Func PathToRawSpec
```go
func PathToRawSpec(pathToFile string) map[string]func() ([]byte, error)
```
Constructs a synthetic filesystem for resolving external references when
loading openapi specifications.



## Types
### Type Backend
```go
type Backend interface {
	// ConfigSummary returns a structured summary of the running configuration.
	ConfigSummary(ctx context.Context) (ConfigSummary, error)
	// ConfigFile returns the path and raw bytes of the loaded YAML config file.
	ConfigFile(ctx context.Context) (path string, content []byte, err error)
	// Pools returns the current state of every configured VM pool.
	Pools(ctx context.Context) ([]PoolStatus, error)
	// Workflows returns every running and recently-completed workflow job, with
	// each job's log artifacts populated.
	Workflows(ctx context.Context) ([]WorkflowStatus, error)
	// Workflow returns a single workflow job by runner-instance name.
	Workflow(ctx context.Context, name string) (WorkflowStatus, bool, error)
	// WorkflowLog opens a log artifact for a workflow job. The caller closes the
	// returned reader.
	WorkflowLog(ctx context.Context, name, artifact string) (io.ReadCloser, LogArtifact, error)
	// Subscribe returns a coalescing change signal and a cancel function. The
	// subscription is also released when ctx is cancelled.
	Subscribe(ctx context.Context) (<-chan struct{}, func())
}
```
Backend supplies the live orchestrator state that the API serves. It is
implemented by the run command over the WorkflowEventHandler and VM pools.
All methods must be safe for concurrent use.


### Type ConfigSummary
```go
type ConfigSummary struct {
	// ConfigFile Path to the YAML config file this orchestrator was started with.
	ConfigFile   string              `json:"config_file"`
	Global       GlobalConfigSummary `json:"global"`
	Pools        []PoolConfigSummary `json:"pools"`
	Repositories []RepositorySummary `json:"repositories"`
	Webhook      WebhookSummary      `json:"webhook"`
}
```
ConfigSummary defines model for ConfigSummary.


### Type DownloadConfigFile200ApplicationyamlResponse
```go
type DownloadConfigFile200ApplicationyamlResponse struct {
	Body          io.Reader
	ContentLength int64
}
```

### Methods

```go
func (response DownloadConfigFile200ApplicationyamlResponse) VisitDownloadConfigFileResponse(w http.ResponseWriter) error
```




### Type DownloadConfigFile404JSONResponse
```go
type DownloadConfigFile404JSONResponse Error
```

### Methods

```go
func (response DownloadConfigFile404JSONResponse) VisitDownloadConfigFileResponse(w http.ResponseWriter) error
```




### Type DownloadConfigFileRequestObject
```go
type DownloadConfigFileRequestObject struct {
}
```


### Type DownloadConfigFileResponseObject
```go
type DownloadConfigFileResponseObject interface {
	VisitDownloadConfigFileResponse(w http.ResponseWriter) error
}
```


### Type DownloadWorkflowLog200ApplicationoctetStreamResponse
```go
type DownloadWorkflowLog200ApplicationoctetStreamResponse struct {
	Body          io.Reader
	ContentLength int64
}
```

### Methods

```go
func (response DownloadWorkflowLog200ApplicationoctetStreamResponse) VisitDownloadWorkflowLogResponse(w http.ResponseWriter) error
```




### Type DownloadWorkflowLog404JSONResponse
```go
type DownloadWorkflowLog404JSONResponse Error
```

### Methods

```go
func (response DownloadWorkflowLog404JSONResponse) VisitDownloadWorkflowLogResponse(w http.ResponseWriter) error
```




### Type DownloadWorkflowLogRequestObject
```go
type DownloadWorkflowLogRequestObject struct {
	Name     string `json:"name"`
	Artifact string `json:"artifact"`
}
```


### Type DownloadWorkflowLogResponseObject
```go
type DownloadWorkflowLogResponseObject interface {
	VisitDownloadWorkflowLogResponse(w http.ResponseWriter) error
}
```


### Type Error
```go
type Error struct {
	Error string `json:"error"`
}
```
Error defines model for Error.


### Type Event
```go
type Event struct {
	Pool      *PoolStatus     `json:"pool,omitempty"`
	Timestamp time.Time       `json:"timestamp"`
	Type      EventType       `json:"type"`
	Workflow  *WorkflowStatus `json:"workflow,omitempty"`
}
```
Event defines model for Event.


### Type EventType
```go
type EventType string
```
EventType defines model for Event.Type.

### Constants
### Hello, Pool, Workflow
```go
Hello EventType = "hello"
Pool EventType = "pool"
Workflow EventType = "workflow"

```
Defines values for EventType.



### Methods

```go
func (e EventType) Valid() bool
```
Valid indicates whether the value is a known member of the EventType enum.




### Type GetConfig200JSONResponse
```go
type GetConfig200JSONResponse ConfigSummary
```

### Methods

```go
func (response GetConfig200JSONResponse) VisitGetConfigResponse(w http.ResponseWriter) error
```




### Type GetConfigRequestObject
```go
type GetConfigRequestObject struct {
}
```


### Type GetConfigResponseObject
```go
type GetConfigResponseObject interface {
	VisitGetConfigResponse(w http.ResponseWriter) error
}
```


### Type GetWorkflow200JSONResponse
```go
type GetWorkflow200JSONResponse WorkflowStatus
```

### Methods

```go
func (response GetWorkflow200JSONResponse) VisitGetWorkflowResponse(w http.ResponseWriter) error
```




### Type GetWorkflow404JSONResponse
```go
type GetWorkflow404JSONResponse Error
```

### Methods

```go
func (response GetWorkflow404JSONResponse) VisitGetWorkflowResponse(w http.ResponseWriter) error
```




### Type GetWorkflowRequestObject
```go
type GetWorkflowRequestObject struct {
	Name string `json:"name"`
}
```


### Type GetWorkflowResponseObject
```go
type GetWorkflowResponseObject interface {
	VisitGetWorkflowResponse(w http.ResponseWriter) error
}
```


### Type GlobalConfigSummary
```go
type GlobalConfigSummary struct {
	CompletionQueueSize *int `json:"completion_queue_size,omitempty"`

	// FailedVmRetentionPeriod Go duration string, e.g. "30m".
	FailedVmRetentionPeriod     *string `json:"failed_vm_retention_period,omitempty"`
	SuccessfulVmRetentionPeriod *string `json:"successful_vm_retention_period,omitempty"`
	TmpDir                      *string `json:"tmp_dir,omitempty"`
}
```
GlobalConfigSummary defines model for GlobalConfigSummary.


### Type InvalidParamFormatError
```go
type InvalidParamFormatError struct {
	ParamName string
	Err       error
}
```

### Methods

```go
func (e *InvalidParamFormatError) Error() string
```


```go
func (e *InvalidParamFormatError) Unwrap() error
```




### Type ListPools200JSONResponse
```go
type ListPools200JSONResponse []PoolStatus
```

### Methods

```go
func (response ListPools200JSONResponse) VisitListPoolsResponse(w http.ResponseWriter) error
```




### Type ListPoolsRequestObject
```go
type ListPoolsRequestObject struct {
}
```


### Type ListPoolsResponseObject
```go
type ListPoolsResponseObject interface {
	VisitListPoolsResponse(w http.ResponseWriter) error
}
```


### Type ListWorkflowLogs200JSONResponse
```go
type ListWorkflowLogs200JSONResponse []LogArtifact
```

### Methods

```go
func (response ListWorkflowLogs200JSONResponse) VisitListWorkflowLogsResponse(w http.ResponseWriter) error
```




### Type ListWorkflowLogs404JSONResponse
```go
type ListWorkflowLogs404JSONResponse Error
```

### Methods

```go
func (response ListWorkflowLogs404JSONResponse) VisitListWorkflowLogsResponse(w http.ResponseWriter) error
```




### Type ListWorkflowLogsRequestObject
```go
type ListWorkflowLogsRequestObject struct {
	Name string `json:"name"`
}
```


### Type ListWorkflowLogsResponseObject
```go
type ListWorkflowLogsResponseObject interface {
	VisitListWorkflowLogsResponse(w http.ResponseWriter) error
}
```


### Type ListWorkflows200JSONResponse
```go
type ListWorkflows200JSONResponse []WorkflowStatus
```

### Methods

```go
func (response ListWorkflows200JSONResponse) VisitListWorkflowsResponse(w http.ResponseWriter) error
```




### Type ListWorkflowsParams
```go
type ListWorkflowsParams struct {
	// State Optional filter by workflow state.
	State *WorkflowState `form:"state,omitempty" json:"state,omitempty"`
}
```
ListWorkflowsParams defines parameters for ListWorkflows.


### Type ListWorkflowsRequestObject
```go
type ListWorkflowsRequestObject struct {
	Params ListWorkflowsParams
}
```


### Type ListWorkflowsResponseObject
```go
type ListWorkflowsResponseObject interface {
	VisitListWorkflowsResponse(w http.ResponseWriter) error
}
```


### Type LogArtifact
```go
type LogArtifact struct {
	ContentType *string `json:"content_type,omitempty"`
	Filename    string  `json:"filename"`

	// Href Relative URL to download this artifact.
	Href string `json:"href"`

	// Id Stable artifact id, e.g. "job" or "diag".
	Id        string `json:"id"`
	SizeBytes *int64 `json:"size_bytes,omitempty"`
}
```
LogArtifact defines model for LogArtifact.


### Type MiddlewareFunc
```go
type MiddlewareFunc func(http.Handler) http.Handler
```


### Type PoolConfigSummary
```go
type PoolConfigSummary struct {
	Image     *string `json:"image,omitempty"`
	Name      *string `json:"name,omitempty"`
	RunnerDir *string `json:"runner_dir,omitempty"`
	Size      *int    `json:"size,omitempty"`
}
```
PoolConfigSummary defines model for PoolConfigSummary.


### Type PoolStatus
```go
type PoolStatus struct {
	Image *string `json:"image,omitempty"`

	// Kind The VM backend kind for this pool, e.g. "tart".
	Kind *string `json:"kind,omitempty"`
	Name string  `json:"name"`

	// Size Configured pool size (target number of warm VMs).
	Size int        `json:"size"`
	Vms  []VMStatus `json:"vms"`
}
```
PoolStatus defines model for PoolStatus.


### Type RepositorySummary
```go
type RepositorySummary struct {
	Owner   *string          `json:"owner,omitempty"`
	Repo    *string          `json:"repo,omitempty"`
	Runners *[]RunnerSummary `json:"runners,omitempty"`
}
```
RepositorySummary defines model for RepositorySummary.


### Type RequiredHeaderError
```go
type RequiredHeaderError struct {
	ParamName string
	Err       error
}
```

### Methods

```go
func (e *RequiredHeaderError) Error() string
```


```go
func (e *RequiredHeaderError) Unwrap() error
```




### Type RequiredParamError
```go
type RequiredParamError struct {
	ParamName string
}
```

### Methods

```go
func (e *RequiredParamError) Error() string
```




### Type RunnerSummary
```go
type RunnerSummary struct {
	Labels     *[]string `json:"labels,omitempty"`
	NamePrefix *string   `json:"name_prefix,omitempty"`
	VmPool     *string   `json:"vm_pool,omitempty"`
}
```
RunnerSummary defines model for RunnerSummary.


### Type ServeMux
```go
type ServeMux interface {
	HandleFunc(pattern string, handler func(http.ResponseWriter, *http.Request))
	http.Handler
}
```
ServeMux is an abstraction of [http.ServeMux].


### Type Server
```go
type Server struct {
	// contains filtered or unexported fields
}
```
Server implements StrictServerInterface over a Backend.

### Functions

```go
func NewServer(backend Backend, opts ...ServerOption) *Server
```
NewServer returns a Server serving state from backend. By default the
embedded SPA build is served; use WithAssets to supply a reloadable asset
filesystem.



### Methods

```go
func (s *Server) APIHandler() http.Handler
```
APIHandler returns an http.Handler that serves the JSON API under BasePath.


```go
func (s *Server) DownloadConfigFile(ctx context.Context, _ DownloadConfigFileRequestObject) (DownloadConfigFileResponseObject, error)
```


```go
func (s *Server) DownloadWorkflowLog(ctx context.Context, request DownloadWorkflowLogRequestObject) (DownloadWorkflowLogResponseObject, error)
```


```go
func (s *Server) GetConfig(ctx context.Context, _ GetConfigRequestObject) (GetConfigResponseObject, error)
```


```go
func (s *Server) GetWorkflow(ctx context.Context, request GetWorkflowRequestObject) (GetWorkflowResponseObject, error)
```


```go
func (s *Server) Handler() http.Handler
```
Handler returns the top-level HTTP handler for the web UI: the JSON API
under BasePath plus the single-page app at the root. The root always
resolves an index.html — the built SPA when present, otherwise the
checked-in placeholder that links to the API (see embed.go).


```go
func (s *Server) ListPools(ctx context.Context, _ ListPoolsRequestObject) (ListPoolsResponseObject, error)
```


```go
func (s *Server) ListWorkflowLogs(ctx context.Context, request ListWorkflowLogsRequestObject) (ListWorkflowLogsResponseObject, error)
```


```go
func (s *Server) ListWorkflows(ctx context.Context, request ListWorkflowsRequestObject) (ListWorkflowsResponseObject, error)
```


```go
func (s *Server) StreamEvents(ctx context.Context, _ StreamEventsRequestObject) (StreamEventsResponseObject, error)
```




### Type ServerInterface
```go
type ServerInterface interface {
	// GetConfig Current orchestrator configuration (structured summary)
	// (GET /config)
	GetConfig(w http.ResponseWriter, r *http.Request)
	// DownloadConfigFile Download the raw YAML configuration file
	// (GET /config/file)
	DownloadConfigFile(w http.ResponseWriter, r *http.Request)
	// StreamEvents Server-Sent Events stream of pool and workflow state changes
	// (GET /events)
	StreamEvents(w http.ResponseWriter, r *http.Request)
	// ListPools List VM pools and the VMs within each pool
	// (GET /pools)
	ListPools(w http.ResponseWriter, r *http.Request)
	// ListWorkflows List running and recently-completed workflow jobs
	// (GET /workflows)
	ListWorkflows(w http.ResponseWriter, r *http.Request, params ListWorkflowsParams)
	// GetWorkflow Get a single workflow job by its runner-instance name
	// (GET /workflows/{name})
	GetWorkflow(w http.ResponseWriter, r *http.Request, name string)
	// ListWorkflowLogs List downloadable log artifacts for a workflow job
	// (GET /workflows/{name}/logs)
	ListWorkflowLogs(w http.ResponseWriter, r *http.Request, name string)
	// DownloadWorkflowLog Download a specific log artifact for a workflow job
	// (GET /workflows/{name}/logs/{artifact})
	DownloadWorkflowLog(w http.ResponseWriter, r *http.Request, name string, artifact string)
}
```
ServerInterface represents all server handlers.

### Functions

```go
func NewStrictHandler(ssi StrictServerInterface, middlewares []StrictMiddlewareFunc) ServerInterface
```


```go
func NewStrictHandlerWithOptions(ssi StrictServerInterface, middlewares []StrictMiddlewareFunc, options StrictHTTPServerOptions) ServerInterface
```




### Type ServerInterfaceWrapper
```go
type ServerInterfaceWrapper struct {
	Handler            ServerInterface
	HandlerMiddlewares []MiddlewareFunc
	ErrorHandlerFunc   func(w http.ResponseWriter, r *http.Request, err error)
}
```
ServerInterfaceWrapper converts contexts to parameters.

### Methods

```go
func (siw *ServerInterfaceWrapper) DownloadConfigFile(w http.ResponseWriter, r *http.Request)
```
DownloadConfigFile operation middleware


```go
func (siw *ServerInterfaceWrapper) DownloadWorkflowLog(w http.ResponseWriter, r *http.Request)
```
DownloadWorkflowLog operation middleware


```go
func (siw *ServerInterfaceWrapper) GetConfig(w http.ResponseWriter, r *http.Request)
```
GetConfig operation middleware


```go
func (siw *ServerInterfaceWrapper) GetWorkflow(w http.ResponseWriter, r *http.Request)
```
GetWorkflow operation middleware


```go
func (siw *ServerInterfaceWrapper) ListPools(w http.ResponseWriter, r *http.Request)
```
ListPools operation middleware


```go
func (siw *ServerInterfaceWrapper) ListWorkflowLogs(w http.ResponseWriter, r *http.Request)
```
ListWorkflowLogs operation middleware


```go
func (siw *ServerInterfaceWrapper) ListWorkflows(w http.ResponseWriter, r *http.Request)
```
ListWorkflows operation middleware


```go
func (siw *ServerInterfaceWrapper) StreamEvents(w http.ResponseWriter, r *http.Request)
```
StreamEvents operation middleware




### Type ServerOption
```go
type ServerOption func(*Server)
```
ServerOption configures a Server.

### Functions

```go
func WithAssets(assets fs.FS) ServerOption
```
WithAssets overrides the SPA asset filesystem served at the root (see
FrontendAssets), e.g. to enable live reloading from the local filesystem.




### Type StdHTTPServerOptions
```go
type StdHTTPServerOptions struct {
	BaseURL          string
	BaseRouter       ServeMux
	Middlewares      []MiddlewareFunc
	ErrorHandlerFunc func(w http.ResponseWriter, r *http.Request, err error)
}
```


### Type StreamEvents200TexteventStreamResponse
```go
type StreamEvents200TexteventStreamResponse struct {
	Body          io.Reader
	ContentLength int64
}
```

### Methods

```go
func (response StreamEvents200TexteventStreamResponse) VisitStreamEventsResponse(w http.ResponseWriter) error
```




### Type StreamEventsRequestObject
```go
type StreamEventsRequestObject struct {
}
```


### Type StreamEventsResponseObject
```go
type StreamEventsResponseObject interface {
	VisitStreamEventsResponse(w http.ResponseWriter) error
}
```


### Type StrictHTTPServerOptions
```go
type StrictHTTPServerOptions struct {
	RequestErrorHandlerFunc  func(w http.ResponseWriter, r *http.Request, err error)
	ResponseErrorHandlerFunc func(w http.ResponseWriter, r *http.Request, err error)
}
```


### Type StrictHandlerFunc
```go
type StrictHandlerFunc func(ctx context.Context, w http.ResponseWriter, r *http.Request, request any) (any, error)
```


### Type StrictMiddlewareFunc
```go
type StrictMiddlewareFunc func(f StrictHandlerFunc, operationID string) StrictHandlerFunc
```


### Type StrictServerInterface
```go
type StrictServerInterface interface {
	// GetConfig Current orchestrator configuration (structured summary)
	// (GET /config)
	GetConfig(ctx context.Context, request GetConfigRequestObject) (GetConfigResponseObject, error)
	// DownloadConfigFile Download the raw YAML configuration file
	// (GET /config/file)
	DownloadConfigFile(ctx context.Context, request DownloadConfigFileRequestObject) (DownloadConfigFileResponseObject, error)
	// StreamEvents Server-Sent Events stream of pool and workflow state changes
	// (GET /events)
	StreamEvents(ctx context.Context, request StreamEventsRequestObject) (StreamEventsResponseObject, error)
	// ListPools List VM pools and the VMs within each pool
	// (GET /pools)
	ListPools(ctx context.Context, request ListPoolsRequestObject) (ListPoolsResponseObject, error)
	// ListWorkflows List running and recently-completed workflow jobs
	// (GET /workflows)
	ListWorkflows(ctx context.Context, request ListWorkflowsRequestObject) (ListWorkflowsResponseObject, error)
	// GetWorkflow Get a single workflow job by its runner-instance name
	// (GET /workflows/{name})
	GetWorkflow(ctx context.Context, request GetWorkflowRequestObject) (GetWorkflowResponseObject, error)
	// ListWorkflowLogs List downloadable log artifacts for a workflow job
	// (GET /workflows/{name}/logs)
	ListWorkflowLogs(ctx context.Context, request ListWorkflowLogsRequestObject) (ListWorkflowLogsResponseObject, error)
	// DownloadWorkflowLog Download a specific log artifact for a workflow job
	// (GET /workflows/{name}/logs/{artifact})
	DownloadWorkflowLog(ctx context.Context, request DownloadWorkflowLogRequestObject) (DownloadWorkflowLogResponseObject, error)
}
```
StrictServerInterface represents all server handlers.


### Type TooManyValuesForParamError
```go
type TooManyValuesForParamError struct {
	ParamName string
	Count     int
}
```

### Methods

```go
func (e *TooManyValuesForParamError) Error() string
```




### Type UnescapedCookieParamError
```go
type UnescapedCookieParamError struct {
	ParamName string
	Err       error
}
```

### Methods

```go
func (e *UnescapedCookieParamError) Error() string
```


```go
func (e *UnescapedCookieParamError) Unwrap() error
```




### Type UnmarshalingParamError
```go
type UnmarshalingParamError struct {
	ParamName string
	Err       error
}
```

### Methods

```go
func (e *UnmarshalingParamError) Error() string
```


```go
func (e *UnmarshalingParamError) Unwrap() error
```




### Type VMState
```go
type VMState string
```
VMState State of a VM as reported by the pool.

### Constants
### VMStateAcquired, VMStateAvailable, VMStateCreating, VMStateDeleted, VMStateFailed, VMStateStaging, VMStateStopped, VMStateUnknown
```go
VMStateAcquired VMState = "acquired"
VMStateAvailable VMState = "available"
VMStateCreating VMState = "creating"
VMStateDeleted VMState = "deleted"
VMStateFailed VMState = "failed"
VMStateStaging VMState = "staging"
VMStateStopped VMState = "stopped"
VMStateUnknown VMState = "unknown"

```
Defines values for VMState.



### Methods

```go
func (e VMState) Valid() bool
```
Valid indicates whether the value is a known member of the VMState enum.




### Type VMStatus
```go
type VMStatus struct {
	CreatedAt *time.Time `json:"created_at,omitempty"`
	Id        string     `json:"id"`

	// LastEvent The most recent pool event kind observed for this VM.
	LastEvent *string `json:"last_event,omitempty"`
	Name      *string `json:"name,omitempty"`
	Pool      *string `json:"pool,omitempty"`

	// State State of a VM as reported by the pool.
	State     VMState    `json:"state"`
	UpdatedAt *time.Time `json:"updated_at,omitempty"`
}
```
VMStatus defines model for VMStatus.


### Type WebhookSummary
```go
type WebhookSummary struct {
	RelayUrl *string `json:"relay_url,omitempty"`
}
```
WebhookSummary defines model for WebhookSummary.


### Type WorkflowState
```go
type WorkflowState string
```
WorkflowState Lifecycle state of a workflow job within the orchestrator.
`vm_completed` means the job finished on the local VM but GitHub has not
yet delivered the completion webhook; `completed` means GitHub acknowledged
completion.

### Constants
### WorkflowStateAcquiring, WorkflowStateCanceled, WorkflowStateCompleted, WorkflowStateFailed, WorkflowStateQueued, WorkflowStateRunning, WorkflowStateVmCompleted
```go
WorkflowStateAcquiring WorkflowState = "acquiring"
WorkflowStateCanceled WorkflowState = "canceled"
WorkflowStateCompleted WorkflowState = "completed"
WorkflowStateFailed WorkflowState = "failed"
WorkflowStateQueued WorkflowState = "queued"
WorkflowStateRunning WorkflowState = "running"
WorkflowStateVmCompleted WorkflowState = "vm_completed"

```
Defines values for WorkflowState.



### Methods

```go
func (e WorkflowState) Valid() bool
```
Valid indicates whether the value is a known member of the WorkflowState
enum.




### Type WorkflowStatus
```go
type WorkflowStatus struct {
	// CompletedAt When GitHub acknowledged completion.
	CompletedAt *time.Time `json:"completed_at,omitempty"`

	// Error Error detail if the job failed or was canceled.
	Error   *string        `json:"error,omitempty"`
	JobId   *int64         `json:"job_id,omitempty"`
	JobName *string        `json:"job_name,omitempty"`
	Labels  *[]string      `json:"labels,omitempty"`
	Logs    *[]LogArtifact `json:"logs,omitempty"`

	// Name The runner instance name assigned by the orchestrator.
	Name         string     `json:"name"`
	Pool         *string    `json:"pool,omitempty"`
	QueuedAt     *time.Time `json:"queued_at,omitempty"`
	RepoFullName *string    `json:"repo_full_name,omitempty"`
	RepoUrl      *string    `json:"repo_url,omitempty"`

	// Result Terminal result if completed, e.g. "Succeeded".
	Result    *string    `json:"result,omitempty"`
	StartedAt *time.Time `json:"started_at,omitempty"`

	// State Lifecycle state of a workflow job within the orchestrator. `vm_completed` means the job finished on the local VM but GitHub has not yet delivered the completion webhook; `completed` means GitHub acknowledged completion.
	State WorkflowState `json:"state"`

	// VmCompletedAt When the job finished running on the local VM.
	VmCompletedAt *time.Time `json:"vm_completed_at,omitempty"`
	VmId          *string    `json:"vm_id,omitempty"`
	WorkflowName  *string    `json:"workflow_name,omitempty"`
}
```
WorkflowStatus defines model for WorkflowStatus.





## External Markdown Files Included Here

### webui — orchestrator management UI + API (claud.README.md)

The `run` command serves this when `web_ui.enabled: true` (see `web_ui.listen_address`
in the config). It exposes an OpenAPI-specified JSON API and an embedded React SPA.

#### Layout

- `openapi.yaml` — the API contract (source of truth for both sides).
- `api.gen.go` — generated Go models + strict server (`go generate ./webui/...`).
- `server.go`, `mount.go`, `embed.go` — handler implementation, routing, SPA embedding.
- `frontend/` — Vite + React + TypeScript SPA. `src/api/types.gen.ts` is generated
  from `openapi.yaml` (`npm run gen`).

#### Regenerating after an API change

Edit `openapi.yaml`, then:

```sh
# Go side (models + strict server)
go generate ./webui/...

# Frontend side (typed client, then build the embedded bundle)
npm --prefix webui/frontend install      # first time only
npm --prefix webui/frontend run gen       # regenerate src/api/types.gen.ts
npm --prefix webui/frontend run build     # regenerate frontend/dist (embedded by Go)

# Rebuild the binary so the new dist is embedded
go build ./...
```

A placeholder `frontend/dist/index.html` is checked in; the build artifacts
(`dist/assets/` and the built `index.html`) are git-ignored, so `go build` always
compiles and serves the placeholder page until the SPA is built. `npm run build`
overwrites the placeholder locally — don't commit the built `index.html` over it,
since it references the git-ignored `assets/` and would render blank on a fresh
checkout.

#### Asset serving and live reload

The compiled SPA is embedded via `go:embed` and served through
`cloudeng.io/webapp/webassets`, so the same binary can either serve the embedded
build or prefer newer files from the local filesystem. Enable reloading in the
`web_ui.reload` config so you can `npm run build` and refresh the browser without
recompiling the Go binary:

```yaml
web_ui:
  enabled: true
  listen_address: 127.0.0.1:8088
  reload:
    reload_enable: true
    reload_new: true
    reload_root: webui   # dir containing frontend/dist (relative to CWD or absolute)
```

`reload_root` is joined with the `frontend/dist` prefix, so it must point at the
`webui` directory (its `frontend/dist` is served in preference to the embedded copy).

#### Frontend dev server

For hot-module reload during heavy UI work, use Vite's dev server instead:

```sh
npm --prefix webui/frontend run dev   # proxies /api to 127.0.0.1:8088
```


